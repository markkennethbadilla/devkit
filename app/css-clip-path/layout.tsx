import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Clip-Path Generator – DevKit",
  description: "Create CSS clip-path shapes visually. Choose from polygon, circle, ellipse, and inset shapes with a live preview.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
