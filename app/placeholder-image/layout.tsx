import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Placeholder Image Generator – DevKit",
  description: "Generate placeholder images with custom dimensions, background color, text color, and labels. Download as PNG instantly.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
