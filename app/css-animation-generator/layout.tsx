import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Animation Generator – DevKit",
  description: "Build CSS keyframe animations visually. Configure timing, easing, iterations, and preview live. Copy the generated code.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
