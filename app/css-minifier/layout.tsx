import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "CSS Minifier / Beautifier — Minify & Format CSS Online",
  description: "Free online CSS minifier and beautifier. Compress CSS to reduce file size or format minified CSS for readability. Runs entirely in your browser.",
  keywords: ["css minifier", "minify css online", "css beautifier", "css formatter", "compress css"],
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
